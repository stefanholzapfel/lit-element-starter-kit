import { css } from 'lit-element';

// language=CSS
export const styles =
    css`
        :host {
            display: block;
            height: 100vh;
            width: 100vw;
            overflow-x: hidden;
            overflow-y: auto;
        }
        
        @media (max-width: 650px) {

        }
    `;
